# Server Configuration
PORT=3000  # Default port for the application

# Admin API Security (REQUIRED for production)
# Generate a secure random key for admin endpoints (/api/admin/*)
# Without this key, admin endpoints will return 503 Service Unavailable
ADMIN_API_KEY=your_secure_api_key_here

# =============================================================================
# MARKET DATA PROVIDER (for real-time prices)
# =============================================================================
# Use "angelone" for FREE real-time market data via Angel One WebSocket
# This can be different from BROKER_PROVIDER - e.g., trade via Zerodha but
# receive free market data from Angel One
# Options: "angelone" (recommended - FREE), "paper" (mock data)
DATA_PROVIDER=angelone

# =============================================================================
# BROKER CONFIGURATION (for order execution)
# =============================================================================
# Options: "paper" (default simulation), "zerodha", or "angelone"
# Note: You can use Zerodha for trading while using Angel One for FREE market data
BROKER_PROVIDER=paper

# Zerodha API Configuration (required if BROKER_PROVIDER=zerodha)
# Get your API key from https://kite.trade/
# NOTE: Zerodha charges ₹2000/month for market data APIs
ZERODHA_API_KEY=your_api_key_here
ZERODHA_API_SECRET=your_api_secret_here

# Zerodha Access Token (optional - will be auto-saved after OAuth login)
# ZERODHA_ACCESS_TOKEN=your_access_token_here

# Zerodha Automated Login (optional - for automatic token generation)
# When set, the broker will automatically log in using these credentials
# eliminating the need for manual OAuth flow
# Get TOTP secret from: Zerodha Profile > Password & Security > Enable external TOTP
ZERODHA_USER_ID=your_user_id_here
ZERODHA_PASSWORD=your_password_here
ZERODHA_TOTP_SECRET=your_totp_secret_here

# Zerodha Trading Configuration
ZERODHA_DEFAULT_EXCHANGE=NSE
ZERODHA_PRODUCT=CNC

# Angel One SmartAPI Configuration (required if BROKER_PROVIDER=angelone or DATA_PROVIDER=angelone)
# Get your API key from https://smartapi.angelbroking.com/
# Angel One provides FREE market data and historical data APIs!
ANGEL_ONE_API_KEY=your_api_key_here
ANGEL_ONE_CLIENT_ID=your_client_id_here
ANGEL_ONE_PASSWORD=your_password_here

# Angel One TOTP Secret (recommended - for automatic 2FA and daily token refresh)
# When set, the system will automatically:
# - Generate TOTP codes for login
# - Refresh tokens daily at 4:30 AM IST (before 5 AM expiry)
# Get this from your authenticator app's setup process or Angel One 2FA settings
ANGEL_ONE_TOTP_SECRET=your_totp_secret_here

# Angel One Trading Configuration
ANGEL_ONE_DEFAULT_EXCHANGE=NSE
ANGEL_ONE_PRODUCT_TYPE=DELIVERY

# Portfolio Storage Configuration
# Backend: "lmdb" (default, recommended) or "file"
PORTFOLIO_BACKEND=lmdb

# Custom storage path (optional)
# PORTFOLIO_STORE=/path/to/storage

# Database Backup Configuration (LMDB only)
BACKUP_INTERVAL_HOURS=24

# Trading Safety Configuration
DRY_RUN=false
MAX_POSITION_SIZE=100000

# Notifications (optional)
ENABLE_NOTIFICATIONS=false
